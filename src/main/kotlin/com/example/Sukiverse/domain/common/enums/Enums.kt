package com.example.Sukiverse.domain.common.enums

enum class Provider { GOOGLE, KAKAO, NAVER }

enum class ContentType { ANIME, JPOP }

enum class LikeType { LIKE, DISLIKE }

enum class SongType { OP, ED, INSERT }

enum class AlbumType { ALBUM, SINGLE, EP }

enum class AnimeStatus { AIRING, COMPLETED, UPCOMING }
