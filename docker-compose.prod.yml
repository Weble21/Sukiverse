services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: sukiverse-app:latest
    container_name: sukiverse-app
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
